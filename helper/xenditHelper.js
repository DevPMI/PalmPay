const { default: axios } = require('axios');
const { XENDIT_BASE_URL, XENDIT_DIRECT_URL, XENDIT_URL_CALBACK, XENDIT_PRIVATEKEY } = require('../../config/constant');

exports.xenditCallAPI = async (req, res) => {
  try {
  } catch (error) {}
};
